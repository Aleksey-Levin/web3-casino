name: rps
supportedstandards: []
events:
  - name: computerChoice
    parameters:
      - name: int
        type: Integer
  - name: gameResult
    parameters:
      - name: int
        type: Integer
  - name: playerBalance
    parameters:
      - name: int
        type: Integer
permissions:
  - methods: '*'
    events:
      - computerChoice
      - playerBalance
      - gameResult
