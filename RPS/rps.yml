name: rps
supportedstandards: []
events:
  - name: computerChoice
    parameters:
      - name: str
        type: String
  - name: playerBalance
    parameters:
      - name: int
        type: Integer
permissions:
  - methods: '*'
    events:
      - computerChoice
      - playerBalance
