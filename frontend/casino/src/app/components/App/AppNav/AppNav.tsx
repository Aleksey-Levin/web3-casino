import {ConnectStateButton} from "../../../web3/connect";

export const AppNav = () => {
    return (
        <div style={{ height: 'max-content '}}>
                <ConnectStateButton/>
        </div>
    );
};